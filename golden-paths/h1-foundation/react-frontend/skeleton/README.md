# ${{ values.name }}

${{ values.description }}

## Overview

A production-ready React single-page application built with TypeScript and Vite, scaffolded by the Three Horizons Accelerator platform.

### Tech Stack

- **React 18** with TypeScript
- **Vite** for development server and optimized production builds
- **React Router** for client-side routing
- **Vitest** for unit testing with coverage
- **ESLint** for code quality

## Getting Started

### Prerequisites

- Node.js 20 or later
- npm 10 or later

### Development Setup

```bash
# Install dependencies
npm install

# Start the development server
npm run dev
```

The application will be available at [http://localhost:3000](http://localhost:3000).

## Available Scripts

| Command           | Description                          |
| ----------------- | ------------------------------------ |
| `npm run dev`     | Start the development server         |
| `npm run build`   | Build for production                 |
| `npm run preview` | Preview the production build locally |
| `npm test`        | Run tests with coverage              |
| `npm run lint`    | Lint source files with ESLint        |

## Build

```bash
npm run build
```

The production build output is in the `dist/` directory.

## Testing

```bash
# Run all tests with coverage
npm test
```

## Deployment

### Docker

```bash
# Build the container image
docker build -t ${{ values.name }}:latest .

# Run the container
docker run -p 8080:80 ${{ values.name }}:latest
```

The application will be served at [http://localhost:8080](http://localhost:8080).

## Project Structure

```
.
├── src/
│   ├── pages/          # Page components
│   ├── App.tsx         # Root application component with routing
│   └── main.tsx        # Application entry point
├── index.html          # HTML entry point
├── vite.config.ts      # Vite configuration
├── tsconfig.json       # TypeScript configuration
├── Dockerfile          # Multi-stage container build
├── catalog-info.yaml   # Backstage catalog descriptor
└── package.json        # Dependencies and scripts
```

## License

This project was generated by the Three Horizons Accelerator platform.
