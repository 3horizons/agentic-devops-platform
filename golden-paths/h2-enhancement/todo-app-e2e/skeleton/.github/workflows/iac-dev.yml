{% raw %}
name: iac-dev

on:
  workflow_dispatch:
    inputs:
      apply:
        description: "Apply Terraform changes"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
  push:
    branches:
      - main
    paths:
      - infrastructure/terraform/**

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6

      - name: Terraform Init
        working-directory: infrastructure/terraform
        run: terraform init

      - name: Terraform Validate
        working-directory: infrastructure/terraform
        run: terraform validate

      - name: Azure login (OIDC)
        id: azure_login
        continue-on-error: true
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: OIDC status
        if: ${{ steps.azure_login.outcome != 'success' }}
        run: |
          echo "::warning::Azure OIDC login failed. OIDC bootstrap may still be in progress."
          echo "::warning::Expected subject: repo:${GITHUB_REPOSITORY}:environment:dev"
          echo "::warning::Terraform Plan and Apply will be skipped. Re-run workflow after OIDC is ready."

      - name: Terraform Plan
        if: ${{ steps.azure_login.outcome == 'success' }}
        working-directory: infrastructure/terraform
        run: terraform plan -var="environment=dev"

      - name: Terraform Apply
        if: ${{ steps.azure_login.outcome == 'success' && github.event_name == 'workflow_dispatch' && inputs.apply == 'true' }}
        working-directory: infrastructure/terraform
        run: terraform apply -auto-approve -var="environment=dev"
{% endraw %}
